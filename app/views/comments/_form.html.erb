<%= form_for([@discussion, (@comment || Comment.new)], remote: true) do |f| %>
  <div class="form-group">
    <h4>Add a Comment</h4>
    <%= f.text_field :body, class: "form-control" %>
    <br>
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>