Hello <%= @user.first_name %>,

Here are the new tasks that were created today for your projects.

  <% @tasks.each do |task| %>
    <% if task.project.user_id ==  @user.id %>
      <%= task.project.title %>
      <%= task.title %>
      <% end %>

  <% end %>

Cheers,

The Tool PM